{
  "name": "orderlyze-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "start:cms": "concurrently \"npm run start\" \"npx decap-server\"",
    "cms": "npx decap-server",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "validate": "node scripts/validate-frontmatter.js",
    "build:index": "node scripts/build-mcp-index.js",
    "build:embeddings": "node scripts/build-embeddings.js",
    "build:all": "npm run validate && npm run build && npm run build:index && npm run build:embeddings",
    "screenshots": "node scripts/take-screenshots.js",
    "screenshots:auto": "node scripts/auto-screenshots.js",
    "screenshots:extended": "node scripts/auto-screenshots-extended.js",
    "woztell:analyze": "node scripts/woztell-faq-analyzer.js"
  },
  "dependencies": {
    "@docusaurus/core": "^3.7.0",
    "@docusaurus/preset-classic": "^3.7.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.1.1",
    "prism-react-renderer": "^2.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.7.0",
    "@docusaurus/types": "^3.7.0",
    "@playwright/test": "^1.57.0",
    "concurrently": "^9.2.1",
    "glob": "^11.0.0",
    "gray-matter": "^4.0.3",
    "playwright": "^1.57.0",
    "typescript": "^5.7.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
